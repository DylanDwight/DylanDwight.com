# Icons

SVG sources for the icons used across the site. They are **inlined** in the HTML
(so there is no runtime icon library or CDN request); these files are kept here as
the canonical, editable source. When you change one, update the inline copies.

All icons: 24×24 viewBox, `currentColor` strokes, `aria-hidden="true"` when decorative.
